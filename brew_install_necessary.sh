#!/usr/bin/env bash

# for sha256sum
brew install coreutils

# for sed
brew install gnu-sed
ln -s /usr/local/bin/gsed /usr/local/bin/sed 

brew install jq
brew install git
brew install gh
brew install wget
brew install curl
brew install python